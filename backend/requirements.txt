fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
pillow>=10.0.0
torch>=2.1.0

# FASHN-VTON is not on PyPI - install from source:
#   git clone https://github.com/fashn-AI/fashn-vton-1.5.git
#   cd fashn-vton-1.5 && pip install -e .
#   python scripts/download_weights.py --weights-dir ./weights

# GPU-accelerated pose detection (DWPose):
onnxruntime-gpu>=1.17.0
# CPU-only environments: comment out the line above and use instead:
# onnxruntime>=1.17.0
